// this component contains hero section with a CTA button
import React, { Component } from 'react'

export class HeroSection extends Component {
  render() {
    return (
      <div>HeroSection</div>
    )
  }
}

export default HeroSection